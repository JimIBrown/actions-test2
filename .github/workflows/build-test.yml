name: test-build

on:
  push:
    branches:
      - main
      - develop
      - feature/*
      - release/*
      - support/*
      - hotfix/*
    paths:
     - src/**
     - build/**
     - .github/workflows/build-test.yml
  pull_request:
  workflow_dispatch:
  
jobs:
  call-workflow:
    uses: JimIBrown/actions-workflows/.github/workflows/docker-image.yml@main
    with:
      dockerRepo: 'DOCKER_REPO'
      projectName: 'ClassLibrary2'